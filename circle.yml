machine:
  node:
    version: 5.5.0
  services:
    - docker
  environment:
    repository: tkqubo/codeclimate-tslint

test:
  override:
    - npm test
  post:
    - npm run test:report

general:
  artifacts:
    - coverage
